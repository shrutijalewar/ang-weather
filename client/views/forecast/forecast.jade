h2 {{title}}

h4 Get Ten Day Forecast
.row
  .col-xs-4
    form(role='form', novalidate, name='forecastForm')
      .form-group
        label(for='zip') ZIP Code
        input.form-control#zip(name='zip', ng-model='zip', ng-pattern='/^[0-9]{5}$/', required, type='text', autofocus, ng-min)
      .form-group
        button.btn.brn-success(type='submit', ng-show='forecastForm.$valid', ng-click='getForecast10day()') Get Forecast
    h6 The zip code you entered is {{zip}}
  .col-xs-8
.row
  .col-xs-12
    .panel-panel-default
      .panel-body
        .forecast(ng-repeat='forecast in forecasts')
          img(ng-src='{{forecast.icon_url}}')
          h6 Description {{forecast.fcttext}}
